# C语言学习笔记：数组入门

## 学习小建议

这部分是数组入门内容，原计划一讲拆成了两讲。学完 0\\6 讲的 C 语言基础，基本就能应对 51 单片机的学习；如果学 STM32，建议再补 7\\9 讲。
基础课不用非得学透再往下走，可以边学单片机边补，遇到卡壳的地方再回头看对应的知识点就行。

## 一维数组

### 数组的作用

最开始存 3 个学生成绩，定义`student1`、`student2`、`student3`三个变量还能应付。但如果人数变多，比如几十个上百个，再一个个写变量就太笨拙了，数组就是用来解决这类批量数据存储问题的。

### 基本用法

声明格式：`数据类型 数组名[元素个数];`
例：`int score[3];` 定义了一个能存 3 个整数的数组。

两种常用初始化：

```c
int num[4] = {0};      // 所有元素初始化为0
int num[4] = {5,7,9,1}; // 依次给每个元素赋值
```

**关键提醒**：数组下标从 0 开始！长度为 3 的数组，可用的元素是`score[0]`、`score[1]`、`score[2]`，不存在`score[3]`，越界访问会出问题。
数组里的单个元素，和普通的同类型变量用法完全一样，可以赋值、放进`if`判断、用`scanf`输入。

### 实战：乱序录入成绩并查询

需求：可以乱序输入学生的学号和对应成绩，之后按学号查分。
核心技巧：学号是从 1 开始数的，数组下标从 0 开始，所以存的时候用`学号 - 1`当下标。
比如输入 2 号学生的成绩，就存在`score[2-1]`也就是`score[1]`里。

用`for`循环控制录入次数，整个代码会非常简洁。以后要增加学生人数，只需要改数组长度和循环的结束条件，不用大改逻辑。

### 课后思考

试着自己写个小程序：让用户输入 10 个整数，按从小到大的顺序输出。就用已经学过的循环、数组、比较大小来实现，先别急着查排序算法。

## 二维数组

一维数组是一排数据，二维数组就有了行和列。
声明：`int arr[5][9];` 代表 5 行 9 列，行下标 0\\4，列下标 0\\8，同样从 0 开始。
赋值例如`arr[3][2] = 6;`，就是给第 3 行第 2 列的元素赋值 6。
用法和一维数组逻辑一致，只是多了一个维度，会用一维的就能上手二维的。

## 内存小知识

逻辑上我们把二维数组想成表格，但在内存里，所有元素其实是连续排成一条线存储的。
至于硬件层面具体怎么存，不用太深究，知道计算机把内存抽象成连续的地址空间就够了。
# C语言零基础学习笔记（第一讲）

## 前期准备

- 打字练十指盲打，推荐金山打字通。可以把微信、QQ 全转到电脑端用，靠日常聊天强制适应，上手很快。键盘选静音款，别买青轴，宿舍、图书馆用不吵人。

- 养成文件管理习惯：软件、资料都存 D 盘，避免 C 盘爆满卡顿。安装路径绝对不能有中文，不然后面编译会出各种莫名错误。文件夹按序号命名分类，找东西很省心。

- 编译器用小熊猫（Red Panda），安装到 D 盘对应目录。打开后默认是 C\+\+，记得手动改成 C 语言。暂时没电脑的话，手机应用商店搜 C 语言 APP 也能临时用。

## 第一个 C 程序

1. 新建工程：文件→新建项目→选「控制台程序」→「C 语言项目」，自定义名称，存到专门的工程文件夹。入门阶段用文件夹管理代码就足够，以后再学 git。

2. 自己的代码写在`return 0;`前面，用`printf("内容");`就能输出文字。

    - **超级易错点**：所有符号（引号、括号、分号）必须是英文的！中文引号会直接标红报错，新手很容易踩这个坑。

3. 点运行按钮，提示未编译就选 yes，屏幕正常显示内容就算跑通了。改双引号里的文字就能修改输出内容，中文也能正常显示。

## 转义序列

一开始想实现换行，复制了两行 printf，结果输出全连在一起；直接在字符串里按回车拆行，直接编译报错。

- 正确换行方法：用转义序列`\n`，放在字符串里需要换行的位置。

- 反斜杠`\`是转义标志，不会直接显示。如果想输出反斜杠本身，要写`\\`。

## 变量与基础运算

### 变量基础

C 语言做计算离不开变量，和数学里设 x、y 的逻辑差不多。

- 现阶段先记两种类型：`int` 存整数，`float` 存小数。

- 变量要声明在`#include`之后、`main`函数之前，**必须先声明再使用**。可以声明时直接赋值，也可以后续修改，不赋值默认值为 0。

### 运算符

- 加减规则和数学一致，乘用`*`，除用`/`，取余用`%`。

- 注意区分：除法是正斜杠`/`，转义是反斜杠`\`，很容易写混。

## 输入输出核心

### printf 输出变量

直接把变量名写双引号里没用，只会原样显示文字，必须用占位符：

- int 对应 `%d`，float 对应 `%f`

- 示例：`printf("体积等于%d", volume);`

- `%f`默认保留 6 位小数，可用`%.2f`控制小数位数，`%8.2f`可控制总显示宽度，位数不足时左侧补空格。

- 占位符和参数要一一对应，数量、类型都得匹配，不然输出会出错。

### scanf 读取用户输入

数值写死在代码里太死板，用 scanf 可以让用户运行时自主输入。

- 写法和 printf 类似，但**变量前必须加取地址符****`&`**（Shift\+7 打出），这个细节特别容易忘。

- **踩坑经验**：代码是从上到下按顺序执行的，计算语句一定要写在输入赋值之后，不然用的还是旧值，结果永远不对。

- 小优化：输入前用 printf 加提示语，比如「请输入长：」，不然用户对着黑屏根本不知道操作。

## 注释与代码结构

### 注释

两种写法：`//` 是单行注释，`/* 内容 */` 是多行注释。注释不参与编译，就是给自己看的备注，写清楚了过段时间回看也能快速看懂。

### 代码基本结构

完整程序的组成逻辑：

1. `#include <stdio.h>`：包含标准输入输出库，printf 和 scanf 都依赖它，不写就用不了。

2. 变量声明区：定义程序要用到的所有变量。

3. `main`函数：有且仅有一个，程序从这里开始运行，所有逻辑都写在大括号内。

4. 每条语句结尾必须有分号，`return 0`暂时不用深究，照着写就行。

## 宏定义

用`#define`可以做等价替换，比如`#define PI 3.14F`，后续代码里写 PI 就等同于 3\.14F。
实用性很强：以后要修改常量数值，只改宏定义这一处就行，不用满代码找数字，也方便维护。

## 变量命名规则

- 只能由英文字母、数字、下划线组成，不能包含 $、@等特殊符号。

- 不能以数字开头，也不能和 int、return 这类 C 语言关键字重名。

- 区分大小写，`a`和`A`是完全不同的两个变量。

- 同类型变量可以一次声明多个，用逗号隔开即可。

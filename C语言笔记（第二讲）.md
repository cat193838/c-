# C语言零基础自学笔记：表达式与选择语句

## 学习方法建议

放弃纸质手写笔记，推荐用 Typora 编写 Markdown 电子笔记，代码块可直接复制复用，效率更高也不易丢失。
学习 C 语言无需死记硬背，**能写出可运行的代码就算掌握**，日常可以查资料、用 AI 工具辅助。主动用好工具，比硬背知识点更重要。

## 语句、表达式与算术运算符

- **语句**：C 语言中以分号 `;` 作为语句结束标志，类似中文的句号。

- **表达式**：`b+c`、`b<c` 这类运算式都属于表达式，多个表达式可组成一条语句。

- 运算符分为一元（如正负号，仅操作一个数）和二元（如加减乘除，操作两个数），和数学概念一致。

- `int` 型与 `float` 型混合运算，结果为 `float` 类型，不会自动取整。

- 取余运算 `%` 仅适用于整数，不能用于浮点数。

- 负数的除法与取余结果无需硬记，实际开发中直接运行代码确认即可。

- 运算符优先级不用背诵，不确定时直接加圆括号 `()` 明确顺序，实际开发中不会写无括号的复杂表达式。

## 赋值运算符

- 赋值号 `=` 作用是将右侧的值赋给左侧变量，左侧只能是变量，不能是常量或运算式。

- 支持连续赋值：`i = k = j = 5`，从右向左依次赋值。

- 复合赋值简写：`a += 2` 等价于 `a = a + 2`，减、乘、除、取余运算同理。

- 自增自减的区别：

    - `b++`：先使用 b 的原值，使用完成后再自增 1

    - `++b`：先让 b 自增 1，再使用自增后的新值

## 关系与逻辑运算符

- 关系运算符（`>` `<` `>=` `<=` `==` `!=`）的运算结果只有 0（假）或 1（真）。

- 注意区分：`==` 是相等判断，`=` 是赋值，二者不要混淆。

- 逻辑运算符：`!`（非，取反）、`&&`（与，两边同真才为真）、`||`（或，有一边为真则为真）。

- 基础选择结构：`if(表达式) 语句; else 语句;`，根据表达式真假执行对应代码。

## if\-else 实战与注意事项

### 基础案例：判断奇偶数

通过 `scanf` 输入整数，利用 `number % 2 == 0` 判断偶数，配合 `if-else` 输出对应结果。

### 花括号的作用

`if` 和 `else` 后默认只能跟随一条语句，若要执行多行代码，必须用花括号 `{}` 包裹为一个整体，否则会出现语法错误或逻辑偏差。

### 多分支与嵌套规则

- 多等级判断可使用 `else if` 级联结构，例如成绩分档场景。

- 嵌套 `if` 时，`else` 永远与**最近的未配对 ****`if`** 匹配。建议嵌套时主动加花括号，避免逻辑歧义。

## 三目运算符、布尔类型与 switch

### 三目运算符

格式：`表达式1 ? 表达式2 : 表达式3`
表达式 1 为真时取表达式 2 的值，为假时取表达式 3 的值，适用于简单二选一赋值，比 `if-else` 更简洁。

### 布尔类型

`bool` 是表示真假的数据类型，取值只能是 0 或 1。赋予非 0 值时，最终都会转为 1。
补充：`if` 判断遵循相同规则，括号内的值非 0 即视为真。

### switch 多分支语句

适合多常量匹配场景，结构比多层 `else if` 更清晰：

1. `switch(变量)` 中的变量与各 `case 常量:` 逐一匹配

2. 匹配成功后会**顺序执行后续所有 case 代码**，因此每个 case 末尾必须加 `break` 跳出

3. 无匹配项时执行 `default` 分支（非必须）

4. 执行逻辑相同的多个 case 可以合并书写，共用一个 `break`

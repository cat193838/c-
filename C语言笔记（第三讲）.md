# C语言学习笔记：循环结构

这是 C 语言第三讲的核心内容，整理了循环结构的关键用法和容易踩坑的地方。

## 一、while 循环

### 基础语法

```c
while(表达式) {
    循环语句
}
```

执行规则：先判断括号里的表达式，C 语言里**非 0 就是真**，条件成立就执行循环里的代码；执行完再回头判断，直到表达式为 0（假）就结束循环。

### 例子：打印 5 到 1

```c
int i = 5;
while(i > 0) {
    printf("%d ", i);
    i--;
}
```

### 重点易错：`i--` 和 `--i`

- **后置自减 ****`i--`**：先拿原值判断条件，判断完再让 i 减 1。
比如`while(i--)`，i 初始为 5 时，循环会执行 5 次；但如果循环里直接打印 i，输出会是`4 3 2 1 0`。

- **前置自减 ****`--i`**：先让 i 减 1，再用新值判断条件。
比如`while(--i)`，i 初始为 5 时，循环只会执行 4 次。

### 补充

- while 的条件不局限于自增自减，比较运算、逻辑运算都可以，最终看结果是不是 0。

- `while(1)` 是死循环，会一直执行，适合需要持续运行的场景。

## 二、do\-while 循环

### 和 while 的核心区别

do\-while 是**先执行一次循环体，再判断条件**，所以不管条件成不成立，循环至少会跑一次。

### 典型适用场景

计算一个整数的位数：如果用 while 循环，输入 0 的时候条件直接不成立，会错误输出 “0 位”；换成 do\-while，就能正确识别 0 也是 1 位数。

## 三、for 循环

### 语法格式

```c
for(初始化; 判断条件; 更新变量) {
    循环语句
}
```

for 相当于把 while 的初始化、判断、变量更新整合到了一起，代码更简洁清晰。

执行顺序：

1. 先执行初始化（整个循环只跑一次）

2. 判断条件，成立就执行循环体

3. 执行完循环体，执行变量更新

4. 回到第 2 步继续判断

### 例子：打印 5 到 1

```c
for(int i = 5; i > 0; i--) {
    printf("%d ", i);
}
```

### 实用细节

1. 三个部分都可以省略不写，但**两个分号绝对不能省**；

2. C99 标准支持在 for 里直接定义变量，属于局部变量，不占用额外全局空间；

3. 同一个部分里可以用逗号分隔，同时写多个操作。

## 四、循环跳转语句

### 1\. break

直接**跳出整个循环**，执行循环后面的代码。
典型用法比如判断素数：从 2 开始挨个试除，只要找到一个能整除的数，就 break 退出循环，说明这个数不是素数。

### 2\. continue

只跳过**本次循环**剩下的代码，直接进入下一轮的条件判断，不会跳出整个循环。

### 3\. goto

可以直接跳转到代码里标记的位置，语法简单但不推荐常用，因为会打乱代码逻辑，后期很难阅读和维护。

## 五、空语句

单独写一个分号`;`就是空语句，什么都不执行，语法上合法，循环里偶尔会用到。

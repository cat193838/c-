# C语言学习笔记：数值、码制与基础数据类型

## 数值与码制

先分清两个核心概念：

- **数值**：纯数学层面的数，比如十进制、二进制，和计算机本身没关系

- **码制**：编码格式，是连接物理存储和逻辑数值的桥梁

理解数据要分三层：物理实在（电路的 0/1 电平）→ 编码格式 → 我们认知的逻辑数值

常见进制：十进制（0\-9）、八进制（0\-7）、十六进制（0\-9、A\-F），都是逢基数进一。

## 位权：进制的核心

不管什么进制，本质都是**位权计数法**：每一位的数字 × 对应位权，加起来就是最终的数。
举个例子：
十进制 632\.45 = 6×10² \+ 3×10¹ \+ 2×10⁰ \+ 4×10⁻¹ \+ 5×10⁻²
二进制 0110\.11 转十进制，算下来结果是 6\.75

进制转换不用死练手算，直接用系统自带的程序员计算器就行，效率高还不容易错。

## 计算机存储基础

- **位（bit）**：最小的存储单位，1 位就是 1 个 0 或者 1

- **字节（Byte）**：8 位 = 1 字节，是最常用的基础存储单元

- **字长**：CPU 一次能处理的数据长度，普通电脑多是 64 位，单片机常见 8 位、16 位

## 十六进制与二进制

十六进制的 1 位，刚好对应二进制的 4 位，所以 2 位十六进制就能表示 1 个字节，写起来比一长串 01 直观多了。
C 语言里用`0x`前缀表示十六进制，比如`0xF0`就对应二进制的`11110000`。

## 整数的存储

### 正整数

直接按进制转换结果存就行。以 1 字节（8 位）为例，能存的正整数范围是 0 \~ 255。
对应三层逻辑：数值 18 → 编码 0x12 → 物理上存的是`00010010`

### 有符号数

把最高位当成符号位：0 代表正数，1 代表负数。一共有三种编码方式：

- 原码：符号位 \+ 数值的绝对值

- 反码：符号位不变，剩下的数值位按位取反

- 补码：反码再加 1（这也是计算机实际在用的编码）

原码和反码都有个问题：存在 “正零” 和 “负零” 两种零，逻辑上有歧义。补码里零的表示唯一，还能把加减法统一成一种运算。

### 补码的核心逻辑

补码的本质是模运算的 “互补”：减去一个数，等于加上它的补数。
就像 12 小时制的钟表，想从 2 点到 1 点，可以逆时针拨 1 格，也可以顺时针拨 11 格，结果是一样的。
计算机用补码，就能把所有减法都转成加法，硬件电路设计起来更简单。

## 小数怎么存

### 定点数

小数点的位置固定死，比如约定好 4 位整数 \+ 4 位小数的格式。
好处是简单，坏处是表示范围小、精度固定。C 语言没有内置的定点类型，要自己约定规则。

### 浮点数

按照科学计数法来存，分成三部分：符号位 \+ 指数位 \+ 尾数位。
小数点的位置由指数决定，是 “浮动” 的，所以叫浮点数。
二进制浮点数有个**隐藏位**的技巧：二进制科学计数法的首位一定是 1，所以可以省掉不存，多挤出来一位空间。

## 整型数据类型汇总

C 语言的整型，说白了就是「字长不同 \+ 有无符号」排列组合出来的：

- `int`：默认是有符号的（等价`signed int`），PC 上一般占 4 字节

- `unsigned int`：无符号整型，没有符号位，全用来存数值，范围更大

- `short`：短整型，通常 2 字节，存小数字的时候更省内存

- `long` / `long long`：长整型，字节更多，能存更大的数

这些修饰符可以自由组合，比如`unsigned long long`。

## 字符类型 char

- `char`默认有符号，占 1 字节，范围是 \-128 \~ 127

- `unsigned char`范围是 0 \~ 255

char 本质上就是个数字：内存里存的是二进制数，用`%c`输出的时候，会通过 ASCII 码表翻译成对应的字符。
比如数值 103，按`%c`输出就会显示字符`G`。
用 1 字节的 char 存字符，不会有字节序的问题，不会出现歧义。

## 浮点类型 float 和 double

- `float`：单精度浮点数，32 位（1 位符号 \+ 8 位指数 \+ 23 位尾数）

- `double`：双精度浮点数，64 位，指数和尾数都更多，精度更高、范围更大

**注意踩坑**：浮点数有精度丢失的问题。十进制小数转二进制的时候，可能会出现无限循环，只能截断存储，所以 0\.1 在内存里其实是个近似值。比较浮点数的时候别直接用`==`，要判断误差范围。